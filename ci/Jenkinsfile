pipeline {
    agent {
          label 'build'
    }
    environment {
        GO111MODULE = 'on'
    }
    stages {
        stage('Compile') {
            steps {
                sh 'hostname && ls'
                sh 'go build'
            }
        }
    }
}